package ProgettoLaboratorioB.main;

/**
 * Enum states define the states of the Application.
 * The states are used to manage flow of the application,
 * through the menus.
 */
public enum SYSTEM_STATE {
    MAIN_MENU,
    LOGIN,
    REGISTER_MENU,
    USER_MENU,
    GUEST_MENU,
    EXIT
}
